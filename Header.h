#include <iostream>
#include <string>
#include <vector>
void Sort3(std::vector<float>& op, const std::string& filename);
void ExamTaskC15(int k);
struct C15 {
    int prod;
    int code;
    int year;
    int month;
};
void Five15(const std::string& inFile, const std::string& outFile);
struct Student {
    std::string surname;
    int score;
};